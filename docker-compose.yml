localdb:
  image: postgres:11.2
  ports:
    - '5432:5432'
  volumes:
    - ./db/dumps:/app-db-dumps
  environment:
    - POSTGRES_USER=angeloantolini
    - POSTGRES_PASSWORD=Skimboard159753
    - POSTGRES_DB=First-docker

testdb:
  image: postgres:11.2
  ports:
    - '5433:5432'
  volumes:
    - ./db/dumps:/app-db-dumps
  environment:
    - POSTGRES_USER=angeloantolini
    - POSTGRES_PASSWORD=Skimboard159753
    - POSTGRES_DB=First-docker
